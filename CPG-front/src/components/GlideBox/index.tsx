import React from "react";
import styles from "./styles.module.scss";

function GlideBox (props){
    return (
        <>
{/* Essa box amarela precisa ter tamanho flexivel e deslizar embaixo do titulo quando passar o mouse sobre ela 

         <div className={styles.GlideBox}>

             {props.title}
             {props.size}
             
             </div>
*/}
        </>
    )
}

export default GlideBox;